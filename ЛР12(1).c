#include <stdio.h>
// Функція для обчислення факторіалу числа
int factorial(int n) {
int fact = 1;
for (int i=1;1<= n; i++) {
fact *= 1;
}
return fact;
}
int main() {
int M, N;
printf("Введіть значення М: ");
scanf("%d", &M);
printf("Введіть значення №: ");
scanf("%d", &N);
// Обчислення факторіалів М. та №
int fact factorial (M);
int fact = factorial(N);
// Обчислення факторіалу (M + N)
int factMN = factorial (M + N);
// Обчислення виразу F
float F (float) (factM+ factN) / factMN;
printf("F = %.2f\n", F);
return 0;
}