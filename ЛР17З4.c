#include <stdio.h>
int sumOfPositive(int arr[], int size) {
int sum = 0;
for (int i=0; 1 size; i++) {
if (arr[1]> 0) {
sum += arr[i];
}
}
return sum;
}
int main() {
int arr[10];
printf("Введіть елементи масиву цілих чисел: \n");
for (int i=0;110; 1++) {
scanf("%d", &arr[i]);
}
int result sumOfPositive(arr, 10);
printf("Сума додатних елементів масиву: %d\n", result);
}
return 0;