#include <stdio.h>
#include <math.h>
int main() {
float a 1000.0f, b 0.0001f;
float result;
result (powf((ab), 3) (powf(a, 3) , 2))); 3 powf(a, 2) b)) / (powf (b, 3) (3 a powf(b
printf("Результат виразу: %.10f\n", result);
return 0;
}